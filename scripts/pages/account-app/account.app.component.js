import template from './account.app.html';
import controller from './account.app.controller';
import './account.app.scss';

let component = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default component;
