import angular from 'angular';
import component from './weight-chart.component';

let module = angular.module('weight-chart', [])

.component('etpWeightChart', component)
  
.name;

export default module;
